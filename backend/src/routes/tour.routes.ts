// import { Router } from 'express';
// import { createTour, deleteTour, getTour, getTours, updateTour } from '../controllers/tour-controller';

// const router = Router();

// router.route('/')
//   .post(createTour)
//   .get(getTours);

// router.route('/:id')
//   .get(getTour)
//   .patch(updateTour)
//   .delete(deleteTour);

// export default router;
