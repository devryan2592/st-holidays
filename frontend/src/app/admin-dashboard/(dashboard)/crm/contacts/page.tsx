import { NextPage } from 'next';

interface ContactsProps {
    // Add your page props here
}

const Contacts: NextPage<ContactsProps> = (props) => {
    return (
        <div>
            <h1> Page</h1>
            
        </div>
    );
};

export default Contacts;
