import { NextPage } from "next";

interface DestinationPageProps {
  // Add your page props here
}

const DestinationPage: NextPage<DestinationPageProps> = (props) => {
  return (
    <div>
      <h1>DestinationPage Page</h1>
      DestinationPage
    </div>
  );
};

export default DestinationPage;
